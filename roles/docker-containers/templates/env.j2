CONFIG={{ home_dir.stdout }}/.config
DOWNLOADS={{ home_dir.stdout }}/downloads
DATA={{ home_dir.stdout }}/data
TZ={{ timezone.stdout }}
PGID={{ user_id.stdout }}
PUID={{ user_gid.stdout }}
DB_PASS=801018
